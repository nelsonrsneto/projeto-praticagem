services:
  - type: cron
    name: praticagem-navios
    schedule: "*/30 * * * *"
    env: python
    buildCommand: pip install -r requirements.txt && playwright install
    startCommand: python main.py